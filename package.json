{
  "name": "cc-vscode-project",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/claude-code-bridge",
    "packages/cc-vscode"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "build:bridge": "cd packages/claude-code-bridge && npm run build",
    "build:extension": "cd packages/cc-vscode && npm run build",
    "build:webview": "cd packages/cc-vscode/webview-ui && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.169",
    "node-pty": "^1.1.0"
  }
}
